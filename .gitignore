node_modules/
dist/
# SQLite lives wherever DATABASE_PATH points — apps/api/data when the dev
# server runs from its own workspace — so match the files, not one directory.
*.db
*.db-shm
*.db-wal
.env
.env.local
*.log
.DS_Store

# build artifacts
*.tsbuildinfo
